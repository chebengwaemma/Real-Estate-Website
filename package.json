{
  "name": "hopeland-global-checkers",
  "private": true,
  "version": "0.0.0",
  "description": "Hopeland Global Checkers — frontend (Vite/React) + backend (Supabase)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend && node scripts/prepare-hostinger.mjs",
    "build:frontend": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "generate:nav-cube": "npm run generate:nav-cube --prefix frontend",
    "stripe:sync": "node backend/scripts/sync-stripe-secrets.mjs",
    "mail:sync": "node backend/scripts/sync-mail-secrets.mjs",
    "functions:deploy": "npm run functions:deploy --prefix backend",
    "deploy:live-email": "npm run deploy:live-email --prefix backend",
    "mail:install": "npm install --prefix backend/server",
    "mail:dev": "npm run dev --prefix backend/server",
    "mail:start": "npm run start --prefix backend/server",
    "postinstall": "npm install --prefix frontend"
  }
}
