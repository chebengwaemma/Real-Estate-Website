# dependencies / build
node_modules/
**/node_modules/
frontend/node_modules/
frontend/dist/
dist/
public_html/

# archives / local delivery zips
*.zip

# local env & secrets — never commit
.env
.env.*
!.env.example
frontend/.env
backend/.env
!frontend/.env.example
!backend/.env.example
# Public VITE_* only — baked into client builds (anon/publishable keys)
!frontend/.env.production

# Hostinger PHP payment secrets (generated at build — never commit)
frontend/public/api/stripe-config.php
frontend/public/api/stripe-config.local.php
frontend/public/api/.env
**/api/stripe-config.php
**/api/stripe-config.local.php
**/api/.env

# logs / OS / editor
*.log
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json

# Vercel / Supabase local
.vercel
backend/supabase/.temp
backend/supabase/.branches

.vercel
.env*
!frontend/.env.production

.vercel
